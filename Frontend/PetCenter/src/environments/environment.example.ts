/**
 * 📋 PLANTILLA — copia este archivo como:
 *   - environment.ts
 *   - environment.development.ts
 *
 * Y reemplaza dogApiKey con tu key real.
 *
 * 🔑 Conseguir la API key:
 *   1. Ve a https://thedogapi.com/signup
 *   2. Regístrate (gratis, solo email)
 *   3. Te llega la key por correo
 *   4. Pégala abajo
 *
 * ⚠ NO subas environment.ts a git (ya está en .gitignore)
 */
export const environment = {
  production: false,
  dogApiKey: 'tu_key_aqui'
};
